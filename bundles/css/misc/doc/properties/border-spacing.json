{
  "name": "border-spacing",
  "description": "The distance that separates adjoining cell borders.",
  "keywords": [
    "css",
    "property",
    "border-spacing",
    "border",
    "spacing"
  ],
  "longDescription": "<p>The lengths specify the distance that separates adjoining cell borders. If one length is specified, it gives both the horizontal and vertical spacing. If two are specified, the first gives the horizontal spacing and the second the vertical spacing. Lengths may not be negative.</p>\n<p>The distance between the table border and the borders of the cells on the edge of the table is the table's padding for that side, plus the relevant border spacing distance. For example, on the right hand side, the distance is padding-right + horizontal border-spacing.</p>\n<p>The width of the table is the distance from the left inner padding edge to the right inner padding edge (including the border spacing but excluding padding and border).</p>\n<p>However, in HTML and XHTML1, the width of the <code>&lt;table&gt;</code> element is the distance from the left border edge to the right border edge.</p>\n\n",
  "syntax": [
    "border-spacing: <length>;"
  ],
  "defaultValue": "0",
  "values": {
    "<length>": {
      "description": "<p>The distance that separates adjoining cell borders.</p>"
    }
  },
  "versions": {
    "doc2": "http://www.w3.org/TR/CSS2/tables.html#propdef-border-spacing",
    "doc21": "http://www.w3.org/TR/CSS21/tables.html#propdef-border-spacing"
  }
}