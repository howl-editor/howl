# Jinja2 Mode for Howl

Provides syntax highlighting for Jinja2 templates.

This mode dynamically determines the base language (e.g., HTML, XML) from the file extension (e.g., `template.html.j2`) and applies the appropriate syntax highlighting along with Jinja2 tag highlighting.